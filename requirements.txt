# Core
anthropic>=0.40.0          # Claude API client (Stage 1 scoring + Stage 2 tailoring)
streamlit>=1.40.0          # local web dashboard / Career CRM
filelock>=3.13.0           # cross-process lock shared by scraper + dashboard

# Scraping stack
python-jobspy>=1.1.70       # LinkedIn + Indeed direct scraping
google-search-results>=2.4.2  # SerpApi client (import name: serpapi)

# Transitive but pinned for stability (jobspy returns DataFrames)
pandas>=2.0.0
